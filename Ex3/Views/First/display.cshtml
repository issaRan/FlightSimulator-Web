@{
    ViewBag.Title = "display";
}
<!DOCTYPE html>
<html>
<body background="~/photo/export-map-share.png" alt="Map" style="background-size:100% 100%; background-attachment:fixed" width:"100%" height:"100%">
    <canvas id="canvas"/>
    <script>
    canvas.setAttribute('width', window.innerWidth);
    canvas.setAttribute('height', window.innerHeight);
    var ctx = canvas.getContext("2d");
    ctx.beginPath();
    var Lon = (@ViewBag.Lon + 180 *(window.innerWidth/360));
    var Lat = (@ViewBag.Lat + 90 * (window.innerHeight / 180));
    ctx.arc(Lon, Lat, 6, 0, 2 * Math.PI);
    ctx.fillStyle = 'red';
    ctx.fill();
    ctx.stroke();
    </script>
</body>
</html>

@*

    var image = new Image();
      image.src = "~/photo/export-map-share.png";
      var canvas = document.getElementById('canvas');
      function initImage() {
          var canvas = document.getElementById('canvas');
          var ctx = canvas.getContext('2d');
          var Mwidth = window.innerWidth;
          var Mheight = window.innerHeight;
          ctx.canvas.width = Mwidth;
          ctx.canvas.height = Mheight;
          ctx.drawImage(image, 0, 0, Mwidth, Mheight);
      }
      let dpr = window.devicePixelRatio;
      onresize = e => {
          if (window.devicePixelRatio !== dpr) {
               setTimeout(_ => dpr = window.devicePixelRatio, 20)
             }else {
               initImage();
            }
      };*@